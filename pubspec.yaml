name: portfolio
description: "Ashwin R — Personal Portfolio App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # Navigation
  go_router: ^14.8.1

  # Animations
  flutter_animate: ^4.5.2

  # Typography
  google_fonts: ^6.2.1

  # Utilities
  url_launcher: ^6.3.1
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/resume.pdf

